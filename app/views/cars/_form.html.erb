<%= form_with(model: (car.new_record? ? [:client, car] : car), local: true) do |form| %>
  <% if car.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(car.errors.count, "error") %> prohibited this car from being saved:</h2>

      <ul>
      <% car.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :number %>
    <%= form.text_field :number %>
  </div>

  <div class="field">
    <%= form.label :model_id %>
    <%= form.select :model_id, options_for_select(@models) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
